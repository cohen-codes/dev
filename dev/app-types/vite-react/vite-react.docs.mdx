---
labels: ['vite', 'dev-server', 'app', 'bit', 'rollup', 'esbuild']
description: 'A React Vite app type for Bit'
---

A React Vite application type for Bit. Supporting SSR and outputs a nodejs server ready output.

## Create an App

Create the app using the Vite app template.

```bash
bit fork bitdev.react/examples/vite-app my-app
bit use my-app
```

You can also add a `my-app.react-vite.ts` file extension to an existing component. See [API reference below](#reference). 

## Run the app

You can list all apps on your workspace using `bit app list`. To run the new app:

```bash
bit run my-app
```

Your app dev server is now listening on the chosen port 🚀. 

## Build the app

To build the app use:

```bash
bit build my-app
```

If using Ripple CI, simply snap and export your components.
Build artifacts can be found on the directory the `bit build` command will output.

```
├── assets
│   ├── index-882a4867.css
│   └── index-a07ae9ce.js
├── index.html
├── manifest.json
├── server-ssr.js
└── server.js
```

You can now use `node` to simply run your app:

```bash
node /path/to/output/server-runner.js 8080
```

You can now checkout your application running on https://localhost:8080. 🎉🎉🎉

## Deploy the app

You can use one of the [Bit deployers](https://bit.dev/docs/getting-started/collaborate/deploy-apps):

- [Netlify](https://bit.cloud/teambit/cloud-providers/deployers/netlify)
- [Vercel](https://bit.cloud/teambit/cloud-providers/deployers/vercel)

If using Ripple CI, don't forget to set your access tokens on the [Ripple CI settings](https://bit.dev/reference/ci/ripple-ci#ripple-ci-secrets).

## Contributing

We welcome contributions! To contribute a component or a change to one of our community scope.

- [Create a lane](https://bit.dev/reference/lanes/create-lane)
- [Import/create and apply your changes](https://bit.dev/docs/getting-started/collaborate/importing-components)
- [Open a change request](https://bit.cloud/bitdev/react/~create-change-request)

## License 
Apache 2.0 license.
